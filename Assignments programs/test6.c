//6. Write a program which takes a character as an input and displays its ASCII code.
#include<stdio.h>
int main(void){
char ch;
printf("Enter the character:");
scanf("%d", &ch);
printf("The ASCII code of this character is %d", ch);

return 0;
}
